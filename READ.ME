#Start the server
docker-compose up -d

#check if it is running
nc -zv localhost 22181
nc -zv localhost 29092

#we can also check the verbose logs while the containers are starting up and verify that the Kafka server is up
docker-compose logs kafka | grep -i started